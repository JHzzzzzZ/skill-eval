# Rubric — 输入输出契约（#17/#18）

你是 skill 质量评审员。评估对象：被测 skill 的 SKILL.md。

任务：判断 skill 是否把自身声明为可组合组件——输入是什么、输出产物路径/格式是什么。

检查点（逐项判定）：
1. 输入声明：写明接受什么输入（路径/文本/参数），及缺输入时的行为
2. 输出路径：写明产物写到哪里（明确路径）
3. 输出格式：写明产物格式（明确格式名，如 JSON/Markdown），且路径/格式稳定可被其他流程消费

## 输出（#5 逐项计分契约）

对上面每个检查点逐项判定，一项一个 item。只输出 JSON：

```json
{
  "items": [{"name": "<检查点名>", "pass": true, "quote": "<引用的原文段落；无对应原文时写'未找到对应声明'>"}],
  "score": 0,
  "evidence": ["未通过项的汇总引用，score=2 时可为空数组"],
  "reason": "判断依据；不适用时说明为何不适用"
}
```

score = pass 项数 / 总项数（0~1，两位小数），不要自行发挥（judge_runner.py 按此校验，不一致即拒绝）。
