# Backlog

按时间倒序记「来源 + 待决问题」；解决后从这里删掉，结论进 `docs/adr/`。

## 待决

- **#15 幂等口径与 #12 不一致**（来源：todo-add 第三次评估 run3 的报告，#15 数据块）：`idem.json` 现在对 15 对相邻跑取**均值**（`ratio 0.1478`）再与 0.5 阈值比；#12 已改成「逐 case 变异系数中位数」（ADR-0012）。两者是同一类问题（跨 case 汇总量 vs 组内量），但 #15 的 pair 本来就构造在同一 case 内，均值不等于错，只是口径不统一 + 对离群对不设防。待决：改逐 case 中位数？改后需新 ADR，且 #15 与前两次报告不可比。未随渲染改动一起做，避免「只是改了展示」这句话失真。
