# PROJECT KNOWLEDGE BASE — docs/adr

Generated: 2026-09-22T13:43:08.578Z

## OVERVIEW

17 条架构决策记录（0001–0017）。0001–0013 来自评估器主线；0014–0016 来自另一条并行线（档位 / 闸门作用域 / 稳定性口径），合并时重新编号避免与已发布的 0010–0013 撞号；其中 0015（闸门作用域）与 0012 是同一决策的两次记录，合并时收敛进 0012、标 superseded（有效决策 16 条）。写"为什么这么定"和"被否决的方案"，不写用法——用法在 `skill-evaluator/reference.md`。

## WHERE TO LOOK

| 要改的东西 | 先读 |
|---|---|
| 沙箱实现（worktree → Docker） | 0001 |
| 评测集冻结 / 跨版本复用 | 0002 |
| 版本号与 `content_sha256` 兜底 | 0003 |
| 指标归约、为什么不合成加权总分 | 0004 |
| 某指标属于哪一期 | 0005 |
| 脚本与测试放哪里（自闭环） | 0006 |
| 沙箱执行载体（pi CLI 子进程，禁 subagent） | 0007 |
| 可移植性闸门的边界 | 0008 |
| 配置面（CLI + env 两层） | 0009 |
| 评估器指纹、`meta.json` 键名 | 0010 |
| 触发集质量 / 跨模型一致率折进 #1/#12 | 0011 |
| #13 扫描口径、排除与抑制 | 0012 |
| #13 五组规则与命中分级（注入检测不走 LLM） | 0013 |
| 五档评估档位（T0–T4）与增量补跑 | 0014 |
| 静态扫描作用域（运行面 vs 测试面） | 0012（原则 + 机制 + 清单，唯一来源）；0015 保留原始论证，已 superseded |
| #12 稳定性按组内（逐 case）变异系数裁决 | 0016 |
| 指标可多源（主源 + 辅证）、#3 行数归静态面 | 0017 |

## CONVENTIONS

- 文件名 `NNNN-kebab-title.md`，编号连续；H1 = 决策标题本身（不写 "ADR-0012: xxx" 前缀）。
- 正文三段：背景一句 → `决定：`（编号列表）→ `原因：`。原因必须写"为什么不是另一个方案"。
- 小节名固定：`## Considered Options`（可选，5/13 有）、`## Consequences`（12/13 有）。Consequences 写代价，不只写收益。
- 全中文；指标统一写 `#N`。
- 新增决策 = 新编号新文件，**不修改旧 ADR**；要改就写新 ADR 声明取代。
- 单篇 15–30 行。超过就说明在写用法而不是写决策。

## ANTI-PATTERNS

- 不在 ADR 里写操作步骤或 CLI 用法——那是 `reference.md` 的职责。
- 不把决策重复进 `SKILL.md`：SKILL.md 只放立即要做的事（ADR-0001 的"定义单点"原则）。
- 不写只列收益的 ADR。0012 明写"扫描面确实变小…会漏判"，0013 明写分组代价——缺代价的 ADR 在本仓算未完成。
- 编号撞车时以**已发布**的编号为准，未发布的一侧重新编号（0014–0016 即此例）；文件名沿用各自原有的中文/英文标题。
- 不把可配置项直接写进代码而不先落 ADR（ADR-0009 要求配置面先有决策）。
